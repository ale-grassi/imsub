#!/bin/sh

branch="$(git rev-parse --abbrev-ref HEAD)"
if [ "$branch" = "main" ]; then
  echo "Blocked: pushing from 'main' is disabled."
  echo "Push from a feature branch and open a PR instead."
  exit 1
fi

exit 0
