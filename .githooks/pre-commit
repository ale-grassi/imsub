#!/bin/sh

branch="$(git rev-parse --abbrev-ref HEAD)"
if [ "$branch" = "main" ]; then
  echo "Blocked: committing directly to 'main' is disabled."
  echo "Create/switch to a feature branch first."
  exit 1
fi

exit 0
